(function( $ ) {
	'use strict';

	$(document).ready(function(){
		$('.nasa-slider').slick({
			dots: true,
			infinite: true,
			speed: 500,
			fade: true,
			cssEase: 'linear'
		});

	});

})( jQuery );
